#include <stdio.h>
#include <stdbool.h>

int main()
{
    double pi = 3.14159;
    float r, l, a;


    printf("PI: %.30lf\n", pi);
    // printf("Enter the radius of the circle: ");
    // scanf("%f", &r);

    // l = 2 * pi * r;
    // a = pi * r * r;

    printf("The circumference of the circle is: %f\n", l);
}